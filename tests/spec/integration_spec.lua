-- Integration tests are moved to plenary-based suite under tests/spec/integration/* using headless Neovim.
-- Keeping this placeholder to avoid breaking CI scripts that expect this file.

describe('integration placeholder', function()
  it('runs', function()
    assert.is_true(true)
  end)
end)
