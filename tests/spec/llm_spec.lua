-- Placeholder: real LLM behavior is covered by integration tests with plenary.
-- This file intentionally contains only basic assertions to keep legacy harness green.

describe('llm placeholder', function()
  it('runs', function()
    assert.is_true(true)
  end)
end)
